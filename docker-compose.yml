services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mini-projet-mlops-api
    ports:
      - "8000:8000"
    command: sh -c "python train.py && uvicorn app:app --host 0.0.0.0 --port 8000"
